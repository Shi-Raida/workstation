# Spec Summary (Lite)

Implement secure SSH key management with Vaultwarden integration to automate SSH key retrieval, configuration generation, and annual key rotation. The system authenticates to self-hosted Vaultwarden at vw.raida.fr, retrieves keys from "SSH Keys" collection, generates secure ~/.ssh/config files, and supports annual manual key rotation with automated server updates.
