# ================================= Vault ================================== #
# This file contains sensitive data that should be encrypted with ansible-vault
# Copy this file to vault.yml and encrypt it using: ansible-vault encrypt vars/vault.yml
#
# IMPORTANT: Never commit the unencrypted version of vault.yml to git!
# The encrypted vault.yml file is safe to commit.

# Git Configuration
# Your personal Git settings - these will be used for commits
git_email: your.email@example.com
git_name: Your Full Name

# Additional Sensitive Variables
# Add any API keys, passwords, or other sensitive data here
# Examples:
# github_token: your_github_personal_access_token
# openai_api_key: your_openai_api_key
# docker_registry_password: your_docker_registry_password
